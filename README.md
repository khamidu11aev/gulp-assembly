# Gulp
# gulp-assembly
+ `"dev": "gulp" ni ishga tushurish komandasi,`
+ `"build": ""`
+ `"deploy": "gulp deployFTP --build",  FTP serverga ulash`
+ `"deploy": "gulp deployFTP --build",  FTP serverga ulash`
 - `test`

